server:
  port: 8080

spring:
  profiles:
    active: dev

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME:${POSTGRES_USER}}
    password: ${SPRING_DATASOURCE_PASSWORD:${POSTGRES_PASSWORD}}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate  # Flyway manages schema, Hibernate only validates
    open-in-view: false
    # Disable JPA schema initialization - Flyway handles it
    defer-datasource-initialization: false

  # Flyway configuration
  # Flyway runs automatically before JPA initialization in Spring Boot
  flyway:
    enabled: true
    baseline-on-migrate: true  # Allows Flyway to work with existing schemas
    baseline-version: 0
    locations: classpath:db/migration
    validate-on-migrate: true
    clean-disabled: true
    out-of-order: false

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    consumer:
      group-id: crypto-exchange-sim
      auto-offset-reset: earliest
    producer:
      properties:
        linger.ms: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never  # Override in dev profile for detailed health info
      show-components: always
  health:
    # Enable health indicators for external services
    redis:
      enabled: true
    db:
      enabled: true
    diskspace:
      enabled: false  # Disable if not needed
    ping:
      enabled: false

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    try-it-out-enabled: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
